{
  "name": "foss-e2e",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "discover": "ts-node -r dotenv/config discover.ts",
    "test": "dotenv -- playwright test",
    "test:auth": "dotenv -- playwright test tests/auth/",
    "test:apps": "dotenv -- playwright test tests/apps/",
    "test:flows": "dotenv -- playwright test tests/flows/",
    "test:security": "dotenv -- playwright test tests/security/",
    "test:all-browsers": "BROWSERS=all dotenv -- playwright test",
    "install:browsers": "playwright install chromium firefox webkit",
    "install:browsers:ci": "playwright install chromium --with-deps",
    "report": "playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.2",
    "dotenv": "^17.4.2",
    "dotenv-cli": "^11.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  }
}
